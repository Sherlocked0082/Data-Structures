//to be done VIMP use of MAXheap and minheap
#include<bits/stdc++.h>
using namespace std;

//Naive method->maintain array in sorted form and as u insert calculate the median  O(n*n)

//Better-> create a augumented BST ... O(n*h)~O(nlogn)


//optimised
/*1.Maintain 2 heaps one will have smaller half elements and other will have greater half elements
  2.ie one will be (smaller{s})maxheap and one will be (greater{g})minheap
  3.if the number of elements are odd then median is 
*/
int main() 
{ 


    return 0;
} 